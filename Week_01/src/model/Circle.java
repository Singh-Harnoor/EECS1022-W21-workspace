package model;

public class Circle {
	/*
	 * Utility methods: no need to create objects in order to use them.
	 */
	public static double getArea(double radius) {
		double area = 0.0;
	    area = 3.14 * radius * radius;
		return area;
		  
	}

}
